#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main(void)
{
    //string name = get_string("What is your name? "); //ask for user's name
    //printf("hello, %s\n", name); //print name
   // printf("%i", ' '/2);
   int bits[3];
   for(int i = 0; i < 3; i++)
   {
        printf("%i", bits[i]);
   }
}